"use client"

import { type FC } from "react"

import { motion } from "framer-motion"

const Profile: FC = () => (
  <motion.section className="container mx-auto">profile</motion.section>
)

Profile.displayName = "Profile"
export default Profile
